<h2>List of ratings</h2>

<ul>
  <% @ratings.each do |rating| %>
    <li> <%= render rating %> 
      <%= button_to 'delete', rating, form: { data: { turbo_confirm: "Are you sure ? "} }, method: :delete %> 
    </li>
  <% end %>

  </ul>

<p><%= link_to "New rating", new_rating_path %></p>